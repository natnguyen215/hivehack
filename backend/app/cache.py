from __future__ import annotations

# Stub — Student 3 replaces this body with real Redis calls.
# Interface is frozen: do not change the signatures.


def get_cached_route(key: str) -> dict | None:
    """Return a cached route dict, or None if not cached."""
    return None


def set_cached_route(key: str, data: dict) -> None:
    """Cache a route dict under the given key."""
    return
